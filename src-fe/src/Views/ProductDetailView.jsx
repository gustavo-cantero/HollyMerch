import ProductDetail from "../Components/ProductDetail/ProductDetail";

const ProductDetailView = ({ product, index }) => {
  return (
    <>
      <ProductDetail />
    </>
  );
};

export default ProductDetailView;
